import LoginComponent from "./LoginComp";

export const metadata = {
    title: "Login Page - Access Your Account",
    description: "User login page for accessing the application",
};

export default function loginPage() {
    return (
        <LoginComponent />
    )
}