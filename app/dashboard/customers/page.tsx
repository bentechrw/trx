import CustomersComponent from "./CustomerComp";

export const metadata = {
    title: "Customers Dashboard",
    description: "Manage your customers effectively",
};

export default function CustomersPage() {
    return (
        <CustomersComponent />
    );
}